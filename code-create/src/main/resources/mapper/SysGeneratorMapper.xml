<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.code.codegen.mapper.SysGeneratorMapper">
	<select id="queryList" resultType="map">
-- 		select table_name tableName, engine, table_comment tableComment, create_time createTime from information_schema.tables
-- 			where table_schema = (select database())
		SELECT name as tableName  FROM sqlite_master WHERE type='table' ORDER BY name;
		<if test="m.tableName != null and m.tableName.trim() != ''">
			and name like concat('%', #{m.tableName}, '%')
		</if>

	</select>

<!--	<select id="queryTable" resultType="map">-->
<!--		select table_name tableName, engine, table_comment tableComment, create_time createTime from information_schema.tables-->
<!--			where table_schema = (select database()) and table_name = #{tableName}-->
<!--	</select>-->


</mapper>
